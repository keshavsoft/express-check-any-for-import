/**
 * Placeholder start function for version 1.
 */
export default function start({ jsFilePath, inCheckLines, showLog }) {
    if (showLog) {
        console.log(`v1 start called on ${jsFilePath}`);
    }
}

/**
 * Placeholder start function for version 2.
 */
export default function start({ jsFilePath, inCheckLines, showLog }) {
    if (showLog) {
        console.log(`v2 start called on ${jsFilePath}`);
    }
}
